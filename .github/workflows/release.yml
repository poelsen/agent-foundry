name: Release

on:
  push:
    branches: [master]
    paths-ignore:
      # Don't release for non-deployable files
      - '*.md'
      - '.gitignore'
      - '.github/**'
      - 'tests/**'
      - 'pyproject.toml'
      - '.claude/**'

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  validate:
    # Skip if commit message contains [skip release]
    if: "!contains(github.event.head_commit.message, '[skip release]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate repo integrity
        run: python3 tools/validate.py

  release:
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Compute CalVer version
        id: version
        run: |
          today=$(date -u +%Y.%m.%d)

          # Find existing tags for today to determine patch number
          # Base tag is just $today, subsequent are $today.1, $today.2, etc.
          if git tag -l "$today" | grep -q .; then
            patch=1
            while git tag -l "${today}.${patch}" | grep -q .; do
              patch=$((patch + 1))
            done
            version="${today}.${patch}"
          else
            version="$today"
          fi

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Computed version: $version"

      - name: Create tag
        run: |
          git tag "${{ steps.version.outputs.version }}"
          git push origin "${{ steps.version.outputs.version }}"

      - name: Build release tarball
        run: |
          version="${{ steps.version.outputs.version }}"

          # Write VERSION file for the tarball (not committed to repo)
          echo "$version" > VERSION

          mkdir -p dist
          tar czf "dist/agent-foundry-${version}.tar.gz" \
            --transform "s,^,agent-foundry-${version}/," \
            --exclude '__pycache__' --exclude '*.pyc' \
            VERSION common/ cli/ tools/setup.py tools/foundry/

      - name: Validate tarball
        run: |
          python3 tools/validate.py --tarball "dist/agent-foundry-${{ steps.version.outputs.version }}.tar.gz"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          version="${{ steps.version.outputs.version }}"
          # Create a stable "latest" filename alongside the versioned one
          cp "dist/agent-foundry-${version}.tar.gz" "dist/agent-foundry-latest.tar.gz"
          gh release create "$version" \
            "dist/agent-foundry-${version}.tar.gz" \
            "dist/agent-foundry-latest.tar.gz" \
            --title "v${version}" \
            --generate-notes
